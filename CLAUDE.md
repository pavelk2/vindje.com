# Project conventions

## Writing style

- Never use em dashes (`—` / `&mdash;`) anywhere: UI copy, docs, commit
  messages, code comments, or chat replies. Use a comma, a colon, or a
  separate sentence instead.
